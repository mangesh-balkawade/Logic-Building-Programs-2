#include<stdio.h>
#include<stdbool.h>
typedef unsigned int unit;
int CountBits(int ino)
{
    

	
}
int main()
{
	int ino1=0,ival1=0,ival2=0,iret=0;
	printf("enter no\n");
	scanf("%d",&ino1);
    iret=CountBits(ino1);
    printf("no of bits are on %d",iret);
    return 0;
}